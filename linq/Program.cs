using data_helper;
using System;
using System.Linq;

namespace linq
{
    class Program
    {
        static void Main(string[] args)
        {
            var personList = PersonData.GetPersonList();    // total 11 people

            // Q1: find the number of people who live in Denver and have age greater than 35. Print results on console window


            // Q2: select all those who have "Rademacher" in their name into a new object containing only "name" and "city" as properties.
            // Print name/city from results on console window (one on each line)
        
        }
    }
}
